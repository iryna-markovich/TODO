import React from "react";
import "./index.css";

const Button = ({ active, children, onClick }) => (
  <button className="container__btn container__btn_combined">rrrrr</button>
);

export default Button;

/*
<button className="container__btn container__btn_regular">
<button className="container__btn container__btn_regular">
<button className="container__btn container__btn_danger">
*/
